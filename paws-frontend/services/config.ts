// Shared configuration for the PAWS app

// Auto-refresh interval synced with Arduino upload rate (5 seconds)
// Arduino: kSampleIntervalMs = 5000UL in arduino-hardware.ino
export const AUTO_REFRESH_INTERVAL_MS = 5000;

// Request timeout for API calls
export const REQUEST_TIMEOUT_MS = 3000;
